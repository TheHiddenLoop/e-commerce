export const selectCart = (state) => state.cart.carts;
export const selectCartCount = (state) => state.cart.count;
export const selectCartTotal = (state) => state.cart.total;
export const selectCardStatus = (state) => state.cart.status;
export const selectCardError = (state) => state.cart.error;
